# りょがサポ 公式ファンサイト（デビュー後リニューアル版）

KO1KEYZ・飯塚亮賀くんを応援する有志ファンダム「りょがサポ」（X: @FD_RYOGA_JP）の情報サイト。
GitHub Pagesでホスティングする静的サイト。運営者は非エンジニアのため、変更は必ず動く状態で完結させること。

## ファイル構成

- `index.html` … トップページ（情報の窓口。詳細は下層に逃がす設計）
- `birthday.html` … センイル投票の詳細ガイド（現在は雛形、GUIDE COMING SOON状態）
- `voting.html` … 音楽番組集票の詳細ガイド（同上）
- `assets/ryoga-01.png` … 公式写真の背景透過素材：クローズアップ（原本・高解像度）
- `assets/ryoga-02.png` … 同：正面バストアップ（原本）
- `assets/ryoga-03.png` … 同：座り全身（原本）
- `assets/ryoga-0X-cut.png` … 上記を幅1200pxに縮小した**Web表示用**。サイトが読むのはこちら。原本を差し替えたら `sips -Z 1200 ryoga-0X.png --out ryoga-0X-cut.png` で作り直す
- `assets/ryoga-0X-cut-sm.png` … 同・幅800pxのスマホ用。`srcset`で自動出し分け。原本更新時は `-cut` と `-cut-sm` の両方を再生成する
- `assets/ryoga-0X.jpg` … 切り抜き前の元写真（現在ページでは未使用。保管用）
- `assets/KO1KEYZ OFFICIAL LOGO.png` … 公式ロゴ原本（2000x2000・余白大）
- `assets/ko1keyz-logo.png` … 上記を2000x500に中央トリミングした表示用。**必ず白地の上に置く**（黒ロゴのため。現在: ヒーローのチップ / Aboutネームプレート / フッターの白プレート）
- `assets/ogp.jpg` … OGP画像（1200x630・JPEG）。3ページの `og:image` から参照。**og:image / og:url は本番の絶対URL（`https://iizukaryogafb.github.io/ryogasapo/...`）に設定済み**。公開URLが変わったら3ページとも差し替えること
- `ogp-gen.html` … OGP画像の再生成ツール（開発用・公開されても実害なし）。`ローカル確認.command` で開き、描画完了後「JPGをダウンロード」→ `assets/ogp.jpg` に上書き。文言・レイアウトは同ファイル内 `draw()` を編集。**写真の後に文字を描かない（顔に文字がかかる事故防止のため、描画順は 文字→写真→スタンプ を維持）**
- `assets/ads/` … 応援広告の写真置き場。原本は `260601osaka.JPG` のような「日付6桁＋場所」名。**サイトが読むのは `-web.jpg`（長辺1400px・品質78）のみ**。原本を置いたら `sips -Z 1400 -s format jpeg -s formatOptions 78 原本.JPG --out 原本-web.jpg` で生成する。ギャラリーは実写真運用中: サムネは4:5トリミング表示、タップでライトボックス（`#lightbox`）が開いて全体表示。新規追加はギャラリーの `.frame` をコピーして**レール先頭に挿入（最新が上）**、`AD_` 番号は古い順の通し。`260525chargespot` は掲載情報未確定のためコメントアウトで保留中（確定したら文言を埋めて番号を振り直す）
- `staging/` … **フル版の保管場所（COMING SOON化前の完全な状態）**。index/birthday/voting の3ファイル入り。資産パスは `../assets/` に書き換え済みで、`/staging/index.html` でそのまま閲覧可能（画面左下にSTAGINGバッジが出る）。企画解禁時は、staging側から該当セクションのHTMLをコピーして本番へ戻す（**その際 `../assets/` → `assets/` に戻し、STAGINGバッジはコピーしない**）

## デザインシステム「grain-sky v3」（ステッカー＆アンチック版）

- パレット（**2026-07更新: メンバーカラーが水色→群青になったため、青系を群青基準に全面調整済み**。明度・彩度＝淡く爽やかな空気は旧版のまま、色相だけシアン寄り→群青寄りに回転）:
  - `--mist:#F3F5FE` / `--sky:#9FB0F6` / `--sky-soft:#D6DDFC` / `--sky-deep:#7D8FEF` / `--cobalt:#3A4FD0`（アクセント主役＝群青） / `--ink:#121C42` / `--ink-soft:#4E5A88` / `--spark:#FFD84D`（黄色は差し色で微量のみ）
  - 旧水色系（#90CFFA / #2B6BE4 等）は**使わない**。グラデ・ベタ影・波線SVGのハードコード値も群青系に置換済みなので、新規要素は必ず上記トークンから引くこと
  - 変数名は歴史的経緯で `--sky` / `--cobalt` のまま（中身は群青系）。リネームすると差分が膨大になるため名前は維持
- フォント（Adobe Fonts ＋ Google Fonts）:
  - 現行キットは**運営者本人が fonts.adobe.com で発行した `yoo6wko`**。読み込みは**ダッシュボード提供のJS埋め込みコード**（`use.typekit.net/yoo6wko.js` を挿し込むスニペット。3ファイルの`<head>`に同一のものが入っている）。**`<link href=".../yoo6wko.css">` の直リンク方式はこのキットでは412になるので使わない**
  - 収録フォント: Obviously ファミリー一式（Wide含む）、Neue Haas Grotesk Display、FOT-筑紫B丸ゴシック Std。**運営者のAdobeアカウントに紐づくため解約・削除すると全ページがフォールバックに落ちる**。キットを作り直した場合は3ファイルのスニペット内 `kitId` を差し替える
  - ※過去にAPIで自動発行したkit（bek1eip / dyk1gsi）は全アクセス412で使えなかった。API発行キットは使わない
  - 主役ディスプレイ = **Obviously Wide / Obviously**（`--font-popw` / `--font-pop`、Bold=700のみ。**font-weight:800は使わない**）。ロゴ・FVタイトル・ゴースト番号・通し番号・チッカー
  - 極小ラベルの黒子 = **Neue Haas Grotesk Display**（`--font-en`、400/500/700）。eyebrow・キャプション・HUD等の10px前後だけ
  - 和文見出し = **FOT-筑紫B丸ゴシック**（`fot-tsukubrdgothic-std`、B=700＋`palt`）
  - フォールバック（＝Adobeが読めない間の実表示）: 英字= Bricolage Grotesque、和文見出し= **Zen Maru Gothic**（丸ゴ。筑紫B丸の代替として意図的に選定）。スタックから削らない
  - 和文本文 = Zen Kaku Gothic New（400/500/700/900、`letter-spacing:.03em`）
  - イタリック差し込み = Instrument Serif italic（控えめに）
  - **手書きフォントは使わない**（不評で撤去済み。復活させない）
- **モチーフ**: 鍵（KO1KEYZ）＝スタンプ中心の鍵穴・ヒーローの鍵ピン（`.key-fab`）・チッカー内の鍵アイコン（`.tk-ic`）・フッター下部の鍵。サッカーボール（亮賀くんのルーツ）＝インタールードの足元ピン（`.ball-fab`）のみ。**これ以上増やさない**（うるさくなる）
- **KO1KEYZ公式ロゴ**は加工禁止・単色黒のまま白地プレートに載せる。顔やタイトルに被せない
- 全ページ共通で `body::after` にSVG feTurbulenceのノイズオーバーレイ（soft-light, opacity .35）
- **設計思想: カード格子を並べない。**罫線・番号・余白の雑誌的レイアウトに、ステッカー的な要素を重ねる
- **ロゴロックアップ**（`.lg-top`＋`.lg-sub`）: 「RYOGA」（Obviously Wide 700・大）の下に「SUPPORT」（Neue Haas 700・極小・広トラッキング）を縦積み。ヘッダー・フッター・イントロ共通。**「RYOGA.SUPPORT」のドット連結表記は使わない（不評で廃止）**
- **FVタイトル（`.hero-title`）は同じロックアップの拡大版**（RYOGA=1em / SUPPORT=.45em・`letter-spacing:.95em`で光学的に同幅）。**RYOGAは白抜き＋コバルトフチ**（`color:#fff`＋`-webkit-text-stroke`＋`paint-order:stroke`＋淡いブルーのぼかしグロー）。写真の暗部（ネクタイ等）に重なっても読めるようにするための機能的な縁取りで、運営者の指定。**ベタ影ずらしや白フチ＋影の袋文字（旧`.pop`）は引き続き禁止**。スマホでは写真下部のマスクフェードの上に重なる設計なので、フェードを外すと可読性が落ちる
- **見出しの文法**: JP大見出し（筑紫B丸B・`clamp(34px,5.4vw,54px)`）の下に英字トラッキングキャプション（`.sec-en`、Neue Haas 700 10.5px `.38em`）。ロゴとして成立する見せ方を保つ。**見出しに波線下線は使わない**（波線はテキストリンクの下線のみ）。`em`はコバルト色のみ
- 共通ボキャブラリー:
  - **ステッカーUI**: 白地＋インク1.5px罫＋**ベタ影ずらし**（`box-shadow:4px 4px 0 ...`、ぼかさない）＋軽い回転。ボタン `.btn`（ホバーで浮き上がり、押すと影0）・eyebrowチップ・hero チップ・タグ類すべてこの語彙。**ぼかしドロップシャドウのボタンは使わない**
  - **袋文字 `.pop`**: ヒーローの「RYOGA / SUPPORT」のみ。白フチ（`-webkit-text-stroke`+`paint-order:stroke`）＋スカイのベタ影。`@supports`フォールバックあり
  - 見出し = しっぽりアンチック＋`em`アクセント（コバルト＋**波線アンダーライン** `--wavy`）＋斜体英語サブ `.sec-en` ＋背景ゴーストナンバー `.sec-ghost`
  - 波線 `--wavy` / `--wavy-sky`（data-uri SVG）: 見出しem・`.tlink`下線・目次ホバーで共通使用
  - リストは罫線行（`.app-row` `.board-row` `.steps`）＋丸矢印 `.circ`（ミニベタ影付き）
  - **アプリ索引はロゴタイル**（`.ar-logo`、50pxステッカー角丸タイル・色面`lt-a/b/c`＋イニシャル）。公式アイコン画像が用意できたら中身を `<img src="assets/apps/xx.png">` に差し替えるだけで角丸・枠は維持される
  - インフォグラフィック: NOWボード（ステータス表＋DAILY 3）、センイル3ステップフロー図（`.flow`）、カムバ週間ストリップ（`.week`）
  - **スケジュールは公式／ファン活動の2分割同時表示**（`.sch-grid`）。タブに戻さない（不評で廃止）
  - **週間ストリップはスマホ（640px以下）で横スクロール禁止**: 1画面幅に収まる圧縮グリッドに切り替わる。`min-width`を復活させない
  - **インタールードは写真のみ**（コピーを置かない。「Shine on」等の見出しは不評で撤去済み）
- **切り抜き写真の構図**（v3の核。アーチ窓は廃止）:
  - ヒーロー = **文字サンド**（RYOGAが写真の後ろ・SUPPORTが手前。z-indexは `.clip.c2`=1 / 写真=2 / `.clip.c3`=3）＋背後にハロー円
  - **スマホ（960px以下）は写真ファースト**: `.hero-visual{order:-1}` で写真がFVの最初に来て、タイトルが負マージンで写真下部に重なる。切り抜き下端は `mask-image` のフェードで溶かす。`.hero-label` はスマホでは非表示。この並びを崩さない
  - インタールード = 座り全身がダーク帯の**角丸の縁にすわる**（`.interlude-photo` z5、`.cut`の`margin-bottom`負値で調整）
  - About = 円形バッジ＋頭が円からはみ出す構図＋**ネームプレート**（切り抜きの下端の切れ目はネームプレートで隠す設計。外すと胴の切断線が見える）
  - **ステッカー・文字・バッジを顔に重ねない**（配置変更時は必ず目視確認）
- シグネチャ要素: 回転スタンプSVG（黄色コア）、チッカー（hero下は`-1.2deg`傾き・フッターは逆走）、ピル型グラスヘッダー（インク罫＋ベタ影）、ドット方眼、シートメニュー（`.sheet`）
- **明暗のリズム**: 昼（ペール）→ 夕空（インタールード）→ ステージ（`#vote` = `.vote-sec.dark`、**角丸48pxで上に重なるネイビー帯**）→ アーカイブ（ペール）→ フッター（角丸44pxのネイビー帯）。ダーク帯の`ON AIR`バーだけ黄色（微量ルール内）
- **境界を跨ぐ要素**: インタールードの切り抜き、フッターの円形「上へ戻る」（`.totop`）。意図的な設計。消さない
- モーション設計（すべて `prefers-reduced-motion` で無効化。リストは index.html のメディアクエリ参照）:
  - イントロカーテン（`.intro`）: 1セッション1回（sessionStorage）。純CSS自走・`pointer-events:none`
  - ヒーロー入場: `.clip > span` の行単位リビール、`--introd` 基準の時差
  - スクロールリビール `.rv` は **`transform`ではなく`translate`プロパティ**を使う（`.frame`の回転等と衝突させないため。`transform`に戻すとガタつくバグが再発する）
  - `[data-stagger]` 内の `.rv` はJSが70ms刻みの時差を付与し、完了後（1.7秒後）にクラスを外す
  - **フロー図・週間表の子要素の初期`opacity:0`は必ず`.rv`付きセレクタで書く**（`.flow.rv .flow-node` / `.week-scroll.rv .wbar`）。`.flow .flow-node{opacity:0}`のように書くと、クラス除去後に再び透明になり「表示した瞬間消える」バグが再発する
  - 週間ストリップのバー伸長・フロー図のノードポップ・シートメニューの時差表示・`.tlink`ホバーの波線マーチ
  - カーソルフォロワー（`pointer:fine`のみ）、東京時刻HUD、ナビのロール文字＋スクロールスパイ、ギャラリーのドラッグスクロール＋右端フェード＋ライトボックス（マウスドラッグ8px超はクリック扱いしない判定入り。崩さない）
- マーカー強調 `.mk` は黄色微量ルールのためページ2箇所まで
- コンセプト: ペールブルー×ノイズ質感で「爽やか＋都会的な洗練」＋ステッカー的な手触り。ターゲットは10代後半〜20代女性。企業的・堅い方向に寄せない
- レスポンシブ（1240px / 960px / 640pxブレーク）と `prefers-reduced-motion` 対応は必ず維持する。JS無効時も `.rv` が見えるよう `<noscript>` スタイルを維持

## 絶対に守るルール

1. **本人画像は公式提供素材のみ**（assets/ の3種とその切り抜き）。番組映像・SNS投稿・ファン撮影・AI生成/加工画像は使用禁止。**顔の改変は一切NG**（背景の切り抜き・マスク処理はOK。ステッカーや文字を顔に重ねるのもNG）
2. フッターとAboutの**「非公式ファンサイト」注記を削除しない**
3. 技術スタックはバニラHTML/CSS/JS（各ページ自己完結）。ビルドツール・フレームワークを勝手に導入しない
4. 日本語本文に英数字以外のラテン/キリル文字を混入させない（出力前に確認）

## 公開状態の管理（2026-07時点）

- **センイル投票（#birthday）と音楽番組の集票（#vote）はCOMING SOON表示中**（`.soon-card`）。フロー図・アプリ索引・週間表などのフル版は `staging/index.html` に保管してあり、企画解禁時にそこから戻す
- **birthday.html / voting.html は本番から一切リンクしていない**（直URLでは開ける骨組み状態）。解禁時にフッターGUIDEの `.fn-soon` をリンクへ戻し、staging版のアプリ索引セクションを復元する
- **「今日のりょがサポ（NOWボード）」セクションは運営者判断で削除済み。復活させない**（セクション番号は 01 SCHEDULE 〜 05 ABOUT に振り直し済み）

## 仮データ（差し替え待ち）

- **公式スケジュールは実データ**（2026-07更新: KCON LA、TOYOTA ARENA TOKYO 4公演、神戸ワールド記念ホール 4公演）。表記ルール: 会場名がタイトル・noteに「開場HH:MM / 開演HH:MM」、1日2公演は「昼公演／夜公演」で改行区切り、タグはLIVE（`tag-live`）。件数変更時はヘッダの `.cnt` も更新
- **ファン活動スケジュールは実データ運用中**（2026-07更新: IDOL CHAMP「Kstyle 最強ビジュアルランキング 決勝投票」7/1〜7/14）。表記ルール: 期間ものは日付セルに「開始日（大）＋ `→ 終了日 曜日`（small）」、**行全体を `<a class="sch-item" target="_blank">` にしてタップで投票ページへ**（`li` は `.rv` のみ）。タイトル先頭にアプリアイコン `.app-ic`（22px角丸。`assets/idolchamp.png` / `choeaedol.webp` / `mnetplus.webp` / `podoal.webp` が格納済み）、noteに「〇〇で投票＋`.sch-open`（TAP TO VOTE）」。件数変更時は `.cnt` も更新
- 投票アプリのラインナップ（最愛アイドル / IDOL CHAMP / Podoal / UPICK / Mnet Plus / Mubeat / STAR PLANET）は**仮置きで、現在はstaging側にのみ存在**。確定したら入れ替えて本番へ復元する
- birthday.html / voting.html の手順は骨組みのみ。確定情報＋スクショで作り込む
- ギャラリーは実写真運用中（2026-07更新: AD_01〜05の5枚掲載）。プレースホルダーは末尾「NEXT PROJECT」カードのみ（意図的に残す）。chargespot分（2026-05-25〜）は掲載場所・媒体の確認待ちでコメントアウト中

## デプロイ

- 公開先: **GitHub Pages** — リポジトリ `iizukaryogafb/ryogasapo`（Public）、Branch: main / (root)
- 公開URL: **https://iizukaryogafb.github.io/ryogasapo/**
- 反映方法: mainにpush（またはGitHub Webの「Add file → Upload files」で同名ファイルを上書きアップロード）→ 数十秒〜数分で反映
- push/アップロード前にローカルで表示確認すること
- 注意: Publicリポジトリなので `staging/` や原本画像も世界公開される（現状は実害なしと判断済み）

### ローカル確認のしかた（重要）

- **`ローカル確認.command` をダブルクリック**するのが正解（サーバーが立ってブラウザが開く）。手動なら `python3 -m http.server 4173` → `http://localhost:4173/`
- **`index.html` を直接ダブルクリック（file://）で開かない**。Adobe Fontsは「Webページからのスタイルシート読み込み」にしか配信されないため、file://ではObviously・筑紫B丸・Neue Haasが出ずフォールバック表示になる
- **フォント監視バナー**: localhost/127.0.0.1で開いた時だけ、Adobe Fontsが読めていないと画面下に紺色の帯が出る（index.htmlのJS）。公開サイトでは出ない。**帯が消えたらAdobe Fonts適用成功**の合図
- フォントが出ない時のチェック順: ①帯の表示内容を見る ②スーパーリロード（Cmd+Shift+R） ③広告ブロッカー拡張をオフ ④シークレットウィンドウ
- 反映の見分け方: ヒーローの「RYOGA」が**横に広い極太**ならObviously適用済み。細身ならフォールバック中
- 開発メモ（Adobe Fontsの検証について。ハマりどころなので必読）:
  - `use.typekit.net` の**CSSエンドポイント**はブラウザらしくないクライアント（curl・fetch・WebFetch・内蔵プレビューのElectron UA）を412で拒否する。412はキットの生死の判定材料にならない
  - 一方**JS埋め込み（xxx.js＋Typekit.load）は内蔵プレビューでも正常に動く**。現方式はこちらなので、AIのスクリーンショット検証も有効
  - 適用判定はcanvasのmeasureText幅比較（実装済みのフォント監視バナー参照）。`document.fonts.check`は誤判定するので使わない。和文の判定に全角文字は使えない（全角は書体によらず等幅）。ラテン字で測る
  - 監視バナーはTypekitの非同期ロードを考慮し、約10秒リトライしても読めない場合のみ表示される
